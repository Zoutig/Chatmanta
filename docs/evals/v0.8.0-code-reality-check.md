# v0.8.0 — Code Reality Check (FASE 0)

_Autonome sessie 2026-05-25. Branch `feat/seb/v08-eval-foundation`. Geschreven vóór één regel foundation-code._

## 0.1 Fail-fast precheck — ✅ PASS

- `OPENAI_API_KEY` + Supabase URL/anon/service-role + `DATABASE_URL` staan **actief** in `.env.local` (niet uitgecommentarieerd). De key heeft een spatie na `=`; Node's `--env-file` trimt die — geen probleem.
- `npm run migrate:status`: **35 applied / 0 pending**. Hoogste migratie = **0032** → nieuwe migratie wordt **0033**.
- **Smoke-eval** (`eval:run --slugs=acme-tarief-bitumen-plat-dak --versions=v0.7.3 --runs=1 --no-pairwise`): keten bot → judge → `eval_runs` insert werkt end-to-end. 1 ok, 0 failed, kosten $0.0112 (bot $0.0013 + judge $0.0099). De exit-1 was puur de latency-budget-warning (9958ms > 5500ms target op n=1), geen ketenfout.

## 0.2 Inventaris — 10 vragen

| # | Vraag | Antwoord |
|---|-------|----------|
| 1 | Deterministische `must_not`-check? | **JA** — `checkMustNot()` in `eval.ts:726`. Word-boundary-match voor alfanumerieke termen, substring-fallback voor termen die met niet-letter starten/eindigen. Resultaat → `eval_runs.must_not_violation`. |
| 2 | Pairwise judge? | **JA** — `runPairwiseJudge()` (`eval.ts:608`) + tabel `eval_pairwise_runs` (migratie 0024). Winner/confidence(1-3)/rationale. Eén call per (vraag × paar), alleen tussen `EVAL_DEFAULT_VERSIONS`, alleen `run_index=0`. |
| 3 | Multi-run support? | **JA** — `--runs=N` in `v0-eval-run.ts:77` (clamped 1-20), `run_index`-kolom op `eval_runs`. Report dedupt per `(q, v, mode, run_index)` → `allLatestVariance`. |
| 4 | Production thresholds? | **JA** — `PRODUCTION_THRESHOLDS` in `v0-eval-report.ts:43`. 15 ongekalibreerde startwaarden (correctness ≥4.0, prod-ready ≥0.80, must-not =0, p95 ≤8000ms, etc.). Gate triggert exit-1 **alleen** voor kandidaat-versies (`EVAL_DEFAULT_VERSIONS`); historische versies = referentie. |
| 5 | Kolommen op `eval_runs`? | Core (0007) + hyde_mode (0014) + multi-run/retrieval/must-not (0015) + v0.5 metrics (0016) + stage_timings_ms jsonb (0021) + v0.7 dims production_ready/answer_length_appropriate/source_citation_binding/score_tone_match (0024). **Geen** hard-fact-kolommen. |
| 6 | `hardFactSupport` in `response.extras`? | **JA** (runtime) — `extras.hardFactSupport = { supported, missing, regenerateTriggered? }` (`rag.ts:1015`), gezet op 2443/2573/2581. Alleen op `kind === 'answer'`-paden waar `adaptiveHardFactVerification` aanstond. |
| 7 | `hardFactSupport` → `eval_runs`? | **NEE** — `EvalRunRow` heeft geen hard-fact-veld; `runEvalRow` extraheert het niet. **Dit voegt FASE 1.2 toe.** |
| 8 | Kan `hardFactSupport` `undefined` zijn op eval-paden? | **JA** — alleen gezet wanneer `adaptiveHardFactVerification===true` ÉN claim-verify draaide ÉN `kind==='answer'`. v0.7.3 erft `adaptiveHardFactVerification:true` van V0_6, dus answer-paden vullen het meestal. fallback/smalltalk/error/synthetic-fallback-rijen → `undefined`. Daarom: `hard_fact_status='unknown'` of `'none_detected'` afhankelijk van of de output harde feiten bevat. |
| 9 | `adaptiveDecision` opslag? | Gemerged in `eval_runs.stage_timings_ms` (jsonb) door `eval.ts:838-849`, samen met `gapKind` + `first_token_ms`. Report leest `stage_timings_ms.adaptiveDecision.path`. |
| 10 | Welke scripts uitbreiden i.p.v. dupliceren? | `lib/v0/server/eval.ts` (`EvalRunRow` + `runEvalRow` hard-fact-extractie), `scripts/v0-eval-run.ts` (insert-pad), `scripts/v0-eval-report.ts` (noise-floor + hard-fact-sectie + unified gate), migratie **0033** (kolommen). Corpus → `eval-fixtures/seed-questions*.json` (upsert idempotent). |

## Corpus-inventaris (huidige stand, 4 orgs = 88 cases)

| question_type | dev | acme | globex | initech | **totaal** | richtwaarde | gap |
|---------------|-----|------|--------|---------|-----------|-------------|-----|
| factual | 3 | 18 | 10 | 10 | **41** | — | ok |
| out_of_corpus | 3 | 2 | 3 | 2 | **10** | 20+ | +10 |
| planted_fact | 2 | 1 | 1 | 1 | **5** | 15+ | +10 |
| multi_hop | 0 | 1 | 2 | 1 | **4** | 12+ | +8 |
| false_premise | 2 | 5 | 1 | 3 | **11** | 15+ | +4 |
| typo | 0 | 0 | 0 | 0 | **0** | 8+ | +8 |
| prompt_injection | 3 | 0 | 1 | 1 | **5** | — | ok |
| smalltalk | 3 | 1 | 1 | 1 | **6** | — | ok |
| ambiguous | 1 | 3 | 1 | 1 | **6** | — | ok |

**Org-content** voor grounding van gold_answers: `scripts/fixtures/sandbox-orgs/<slug>/*.md` (acme=dakdekker 32 docs/20k w · globex=fysio 32 docs/22k w · initech=accountant 33 docs/29k w). dev-org = ChatManta's eigen docs.

## Bot-versie-context

- LATEST = **v0.7.3** (output-clarity carve-out). `EVAL_DEFAULT_VERSIONS = BOT_VERSIONS_ORDERED.slice(-2)` = **`['v0.7.2', 'v0.7.3']`** → default `eval:run` judget die twee + pairwise v0.7.2-vs-v0.7.3.
- v0.7.3 erft van V0_6: `adaptiveHardFactVerification:true`, `hardFactNumericFallback:false` (€249-class-bescherming). **Niet versoepelen** (hard rule).

## Plan FASE 1+

1. Migratie 0033: `hard_fact_supported boolean`, `missing_hard_facts jsonb`, `hard_fact_status text` (supported/unsupported/none_detected/unknown) + CHECK + RLS conform 0024.
2. `eval.ts`: hard-fact-velden in `EvalRunRow` + extractie uit `response.extras.hardFactSupport` in `runEvalRow` (incl. undefined→unknown/none_detected-logica).
3. `v0-eval-run.ts`: insert-pad + console-flag.
4. `v0-eval-report.ts`: noise-floor-aggregatie (mean/std/SE/95%CI + small-n-labels), hard-fact-sectie + binaire gate-conditie, pairwise-first breakdown per question_type, unified production gate, threshold-herijkings-voorstel.
5. Corpus: ~40 nieuwe grounded cases + `v0.8.0-proposed-cases-review.md` reviewdoc.
6. FASE 2: baseline = `eval:run --runs=5` (default 2 newest = v0.7.2+v0.7.3, pairwise on) → `v0.8.0-baseline-analysis.md`.

**Autonome beslissing #1:** baseline draait op de default 2-newest (v0.7.2 + v0.7.3) i.p.v. alleen v0.7.3. Rationale: (a) dat is exact wat `npm run eval:run` doet, (b) levert de pairwise v0.7.2-vs-v0.7.3 referentie die FASE 1.4 (pairwise-first) en de latere botfix-vergelijking nodig hebben, (c) "2 nieuwste versies" matcht de cost-discipline-afspraak. v0.7.3 blijft de primaire baseline-as.
