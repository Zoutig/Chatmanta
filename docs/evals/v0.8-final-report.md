# v0.8 — Eindrapport (autonome sessie)

_2026-05-25, onbewaakte sessie. Branch `feat/seb/v08-eval-foundation`. 6 commits. Geen botgedrag gewijzigd; v0.7.3 byte-identiek._

> **⚠️ UPDATE (vervolgsessie, zelfde dag) — Optie E overruled.** In een aansluitende sessie is de v0.8.1 anti-adoptie-botfix alsnog gebouwd op `feat/seb/v081-anti-adoption` (gestapeld op deze branch). De fix is na een schone re-eval **gepromoveerd tot LATEST**. §5–§7 + §10 hieronder beschrijven de oorspronkelijke meet-release-uitkomst; de actuele stand staat in **§11 (v0.8.1 update)** en in `docs/evals/v0.8.1-analysis.md` + `docs/V0_8_SESSIE_OVERZICHT.md`._

## 1. Executive summary

**Doel:** eerst de meetlat betrouwbaar maken (v0.8.0 eval-foundation), baseline draaien, en alléén bij een dominant data-signaal één gerichte botfix.

**Gebouwd:** volledige eval-foundation (hard-facts in eval, noise-floor met CI's, pairwise-per-type, unified gate, threshold-voorstel, corpus 88→128) + baseline op v0.7.2/v0.7.3 + een **gevalideerde meetfix** voor een must-not-meetbug. **Geen botfix** — de beslisregel wijst die niet aan (zie §5).

**Production-ready?** Nee. Status = **`EVAL READY, BOT NOT READY`** (§7). De meetlat staat; de bot heeft een reële anti-hallucinatie-zwakte (planted_fact adoptie) en haalt de productie-drempels niet.

**Grootste resterende blocker:** planted_fact adoptie — de bot bevestigt een geplant onwaar feit (verzonnen naam/prijs) i.p.v. te corrigeren (C0/G0, cross-org, reproduceerbaar). Bucket-n=16 (<20) → eerst versterken, dan fixen.

## 2. Code reality check (FASE 0)

Smoke-eval bevestigde de keten bot→judge→`eval_runs`. Inventaris: `checkMustNot`, `runPairwiseJudge` + `eval_pairwise_runs`, `--runs=N` multi-run, `PRODUCTION_THRESHOLDS`, `adaptiveDecision` in `stage_timings_ms` — **bestonden al**. `response.extras.hardFactSupport` bestond in runtime maar werd **niet** naar `eval_runs` geschreven (= de gap die FASE 1.2 dichtte). Niets dubbel gebouwd; alles uitgebreid. Detail: `docs/evals/v0.8.0-code-reality-check.md`.

## 3. Eval Foundation changes (FASE 1)

- **Migratie 0033**: `hard_fact_supported`/`missing_hard_facts`/`hard_fact_status` op `eval_runs` (RLS via bestaande 0007-policy). `runEvalRow` extraheert `hardFactSupport`; undefined-paden → `unknown` (nooit auto-PASS) of `none_detected`.
- **Noise-floor**: per versie × metric mean/std/SE/95%-CI + n_cases/n_runs + small-n-labels; delta-verdict via CI-overlap.
- **Hard-fact-sectie** + **binaire gate** (`unsupported`=0) + per-question_type breakdown.
- **Pairwise-first**: win-rate per question_type.
- **Threshold-herijkings-voorstel**: `recommended = max(safety_floor, 95%CI-lo)`; safety-gates blijven HARD.
- **Corpus 88→128**: +40 grounded cases (typo 0→8, out_of_corpus 10→20, planted_fact 5→15, multi_hop 4→12, false_premise 11→15) + reviewspoor met 3 REVIEW_NEEDED-flags.
- **Geschatte spend vóór run** (FASE 1.6): ~$15 (raming); werkelijk $18.65 (corpus bleek 180 i.p.v. 128 in DB).

## 4. Baseline v0.7.2 / v0.7.3 (FASE 2)

| versie | C | P | G | overall | prod-ready |
|--------|---|---|---|---------|-----------|
| v0.7.2 | 3.38 | 3.47 | 3.60 | 3.48 | 44% |
| v0.7.3 | 3.40 | 3.49 | 3.64 | 3.51 | 44% |

Pairwise (n=180): v0.7.3 39% / v0.7.2 33% / tie 27% = **gelijk-op**, binnen noise (CI's overlappen). Enige beslissende winst v0.7.3: `planted_fact` (8 vs 1). Beide kandidaten **falen** de (aspirational) productie-drempels.

**Top failure-buckets:** (1) planted_fact adoptie (genuine, dominant); (2) legacy dev-org cruft (52 stale cases — vertekenen de telling); (3) ambiguous/false_premise over-eagerness. Top-20 failing slugs + diagnose-labels: `docs/evals/v0.8.0-baseline-analysis.md`.

**Drie meet-artefacten** (de meetlat was nog niet 100% betrouwbaar):
1. `must_not_contain` deny-by-naming false-positives → **GEFIXT** (adoptie-frase-patroon) + gevalideerd op opgeslagen antwoorden (echte adopties blijven gevangen).
2. hard-fact-verifier flagt correcte rekenkunde (Vpb-totalen niet letterlijk in sources) → gedocumenteerd, niet versoepeld.
3. report-snapshot mengt hyde-mode/history (v0.7.2 n=21-artefact) → headline uit run-log.

## 5. Botfix-decision (FASE 3)

**Optie E — geen botfix deze sessie.** De beslisregel vereist een dominant signaal buiten de noise-band op een bucket met **voldoende n**. planted_fact is effect-sterk (C0/G0, 5/5 runs, 3 orgs) maar n=16 < 20. Voorwaarde (d) faalt; gecombineerd met context-verbruik (A4.5) is meet-release de juiste uitkomst. Niet gekozen alternatieven: parallelle answerability-gate (hard rule), prompt-only refusal-fix (hard rule), model-switch/embedding-upgrade, regenerate OR→AND, numericFallback versoepelen. Detail + acceptatiecriteria: `docs/evals/v0.8.1-decision-memo.md`.

## 6. Botfix-resultaat

N.v.t. — geen botfix gebouwd (Optie E).

## 7. Production-readiness-verdict

### `EVAL READY, BOT NOT READY`

De eval-infrastructuur is na deze sessie betrouwbaarder en breder (hard-facts gemeten, noise-floor + CI's, pairwise-first, must-not-meetbug gefixt + gevalideerd, corpus +40 grounded cases). Maar de bot (v0.7.3, LATEST) haalt de productie-drempels niet en heeft een reproduceerbare anti-hallucinatie-zwakte (planted_fact adoptie). Niet klaar voor betalende klanten; wél een solide, eerlijke meetbasis om de volgende gerichte verbetering data-gedreven te sturen.

## 8. Autonome beslissingen

1. **Baseline op default 2-newest** (v0.7.2 + v0.7.3) i.p.v. alleen v0.7.3 — levert de pairwise-referentie die FASE 1.4/5 nodig hebben; matcht cost-discipline ("2 nieuwste").
2. **Run op 180 vragen** (incl. 52 legacy dev-org) i.p.v. DB opschonen — `eval_runs.question_id` kan `ON DELETE CASCADE` zijn → stale questions verwijderen zou eval-history vernietigen (onomkeerbaar, niet gevraagd). Legacy apart gemarkeerd in de analyse.
3. **must_not deny-by-naming meetbug gefixt** in client-org cases (foundation-correctheid) + gevalideerd op opgeslagen bot-antwoorden zonder API-kosten.
4. **Grounding=0 gebruikt** om genuine planted-adoptie te onderscheiden van judge-artefact (correcte weigering laag gescoord).
5. **Legacy dev-org eval_questions NIET verwijderd** (cascade-risico) — gedocumenteerd voor cleanup i.p.v. destructief opruimen.
6. **Optie E** (geen botfix) per deterministische beslisregel.

## 9. Werkelijke eval-spend

**$18.65** (bot $2.82 + absolute judge $15.25 + pairwise $0.58) voor de baseline (1800 jobs). Plus ~$0.02 smoke-evals. Re-derivatie/queries van must-not + top-failing = **$0** (herberekend op opgeslagen antwoorden, geen LLM-calls). **Totaal ≈ $18.7.** Budget extern bewaakt via OpenAI-dashboard; niet zelf gethrottled.

## 10. Next steps (geprioriteerd, max 5)

1. **planted_fact bucket → n≥20** (4-6 extra grounded cases), dan de anti-adoptie-fix bouwen (hard-fact/claim-verificatie uitbreiden naar history-geïntroduceerde namen — geconsolideerd, geen nieuwe laag).
2. **Legacy dev-org corpus opschonen** (52 stale cases re-seeden/verwijderen) — verwijdert de grootste vertekening in de failure-telling.
3. **Threshold-herijking toepassen** (aspirational drempels → noise-gebaseerde waarden; safety-gates hard houden).
4. **Hard-fact-verifier arithmetic-aware** maken (Optie D) zodat correcte rekensommen niet als `unsupported` tellen.
5. **Ambiguous/false_premise over-eagerness** meten en adresseren (bot antwoordt waar doorvragen/weigeren hoort).

## 11. v0.8.1 update — anti-adoptie gebouwd + gepromoveerd (vervolgsessie)

Optie E (§5) is in een aansluitende sessie overruled: de gerichte anti-adoptie-fix is gebouwd en, na een schone re-eval, **gepromoveerd tot LATEST**. Branch `feat/seb/v081-anti-adoption`.

**Gebouwd:** `historyEntityVerification` (append-only `v0.8.1 = {...v0.7.3}`). Detector `history-entities.ts` flagt een persoonsnaam uit de chat-history die niet in de sources staat maar bevestigend in het antwoord verschijnt → bestaande regenerate vervangt het antwoord door een **deterministisch** weiger-template (LLM-regenerate-met-prompt bleek onbetrouwbaar — bevestigt de prompt-only-hard-rule). v0.7.3 byte-identiek. Plus: 6 grounded planted_fact-cases (bucket n→22) en een **eval-meetbug gefixt** (eval mat `answer-done` i.p.v. het `replacement`-event → alle regenerate-fixes waren onzichtbaar).

**Re-eval (v0.7.3 vs v0.8.1, n=186, runs=1, $4.37):**
- Must-not 11→8, **0 nieuwe**; gefixt: mark-visser, roel-rb, injection-ignore. (`companion-frank` rest = deny-by-naming meetartefact, correcte weigering.)
- planted_fact-bucket 2.91→3.39 (+0.48), violations 4→2, pairwise vóór v0.8.1 (50% vs 32%, n=22).
- factual 3.46→3.63 (n=60), goedkoper + sneller. Aggregaat-deltas binnen noise (verwacht voor smalle fix; must-not-winst is deterministisch).
- Productie-gate nog niet gehaald (v0.8.1 faalt 13 vs 14 drempels) → **BOT-NOT-READY blijft**.

**Verdict:** PROMOVEREN — schone, deterministische, mechanistisch sluitende fix; het ene getripte criterium (unsupported-HF overall +1) is ruis buiten v0.8.1's effectgebied (omlaag in de doel-bucket). Detail: `docs/evals/v0.8.1-analysis.md`. **Residu → v0.8.2:** brand-name (hetzner) + pronoun-adoptie.
